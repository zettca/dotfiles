#!/bin/sh

# git
git config --global user.name <NAME>
git config --global user.email <EMAIL>
git config --global status.hints false
git config --global core.editor vim

# npm
npm config set prefix ~/.npm-packages

